# 阿凱老師教育工具集 · 未來優化路線圖 v3.6.100+

> 產出日期：2026-07-28
> 基準版本：`v3.6.100`（122 個工具 / 123 篇手寫長文）
> 觸發來源：#122 上架時踩到 `audienceFit.departments` 陷阱、訪客進度條比例語意錯誤、
> 回訪重問功能上線、本機錯誤誤觸正式告警 —— 這四件事各自暴露一條可以延伸的改良軸線。

---

## 🎯 這份清單怎麼來的

不是憑空發想。四個本次真實踩到的問題，各自往上追一層根因，就長出一條軸線：

| 本次踩到的 | 往上一層的根因 | 延伸出的軸線 |
|---|---|---|
| `departments` 填了就把導師／科任整組排除 | 推薦引擎的資格語意沒有防呆，錯了也不會有人發現 | **A. 推薦引擎可信度** |
| 進度條「5,448 人卻顯示 8%」 | 數字正確 ≠ 訪客讀得懂，缺少「體感檢查」 | **B. 數據體感與敘事** |
| 回訪重問剛做完，效果未知 | 新的 UX 機制上線後沒有量測回路 | **C. 回訪與留存閉環** |
| 本機錯誤推到正式 Google Chat | 告警管線沒有分級，噪音與真事件同一條 | **D. 告警與可觀測性品質** |
| 本機 `npm run dev` 起不來 | 開發環境沒有「一定跑得起來」的保證 | **E. 開發者體驗（DX）** |

與既有的 `ADM-P0／P1` Admin 後台清單（見 `PROGRESS.md` 的 `2026-07-21` 段落）**互補不重疊**：
那份談的是後台治理與安全，這份談的是「面向訪客的推薦品質、體感與工程健康度」。

---

## 🚀 P0 立即可做（1–3 個 session 完成）

### ~~P0-1 · `audienceFit` 資格語意防呆~~ ✅ 已完成（v3.6.101）

**問題**：`isEligible()` 目前的寫法是——

```ts
if (fit.departments !== undefined) {
  return profile.teacherRole === 'admin'
    && profile.department !== undefined
    && fit.departments.includes(profile.department);
}
```

只要 `departments` 有值，`teacherRoles` 裡的 `homeroom` / `subject` **完全被忽略**。
這在語意上是「departments 是排除性欄位」，但欄位名字看起來像「額外加註的處室」。
#122 就是這樣默默把國小導師與科任全部關在門外，而 `npm run validate:audience` **不會報錯**。

**建議做法（三選一，可疊加）**：

1. **改語意（推薦）**：`departments` 只在 `teacherRole === 'admin'` 時才當作過濾條件，
   非 admin 職務仍走 `teacherRoles` 判定。這樣「老師＋行政都能用」的工具可以照實填處室。
2. **加驗證**：`validate-audience-fit.ts` 增加規則——若 `teacherRoles` 含 `homeroom`／`subject`
   卻同時填了 `departments`，直接 fail 並提示「這會排除掉導師與科任」。
3. **加覆蓋率測試**：每個工具至少要能在 1 個實際 profile 的推薦結果中出現，否則 CI 擋下。

**🔴 這不是理論風險，正式站已經有第二個案例**：
2026-07-28 12:27 補完的 **#80 114石小教師會議報告集合站**，填了
`teacherRoles: ['homeroom','subject','admin']` ＋ `departments: ['academic','student-affairs']`，
實測結果——

```
❌ 國小導師 → 撈不到 #80（reasons.homeroom 寫了也沒用）
❌ 國小科任 → 撈不到 #80（reasons.subject 寫了也沒用）
✅ 教務行政 → 撈得到 #80 (rank 7)
```

那兩條精心寫好的推薦理由是**死程式碼**，而且 `validate:audience` 全綠、CI 全綠、沒有任何警告。
一天之內同一個陷阱踩兩次（#122 是實測才發現、#80 是上線後才發現），
足以說明「靠人記得」這條防線已經失效。

**驗收**：新增一個工具若填出「沒有任何客群撈得到」的組合，CI 會紅燈而不是默默上線；
現有 122 個工具全數掃一次，列出所有「reasons 寫了但實際撈不到」的組合。
**成本**：S–M（改語意需回頭複查所有已填 `departments` 的工具）

---

### ~~P0-2 · 補完 #80 的 `audienceFit`，解鎖 22 profile 覆蓋率檢查~~ ✅ 已完成

**2026-07-28 12:27 由另一個 session 完成**（commit `69cc25a`）。
`validate:audience` 不再印略過訊息，22 個 profile 覆蓋率檢查已正式生效，
`painPoint coverage` 也開始輸出（最低的三項：`professional-learning=3`、
`presentation=4`、`reading-literacy=4`、`accessibility=4`）。

**但留下兩件事**：
1. #80 本身踩到 P0-1 的 `departments` 陷阱（詳見 P0-1），導師與科任撈不到。
2. 覆蓋率報表現在跑得出來了 → 直接餵給 **P2-4 覆蓋熱力圖**，
   `professional-learning`（專業成長）只有 3 個工具，是下一批工具最明確的開發缺口。

---

### ~~P0-3 · 回訪重問的成效量測~~ ✅ 已完成（v3.6.101）

v3.6.99 已經埋了 `audience_wizard_opened` 帶 `mode=first_time / re_prompt`，
但還沒有地方看得到結果。至少要能回答三個問題：

- 重問的**完成率** vs 首次引導的完成率（重問會不會被當成打擾？）
- 重問後的推薦**點擊率**是否高於首次（換一批新工具真的有效嗎？）
- 「這次先跳過」的比例，以及跳過後 3 天再問時的態度變化

**建議**：在既有的 `RecommendationStatsPanel` 加一組 `mode` 切片，
或先做最小版本——每週 Google Chat 摘要帶一行「本週重問 N 次，完成 M 次，CTR X%」。

**驗收**：能用數據決定「7 天」這個間隔要不要調成 14 天或 30 天。
**成本**：S–M

---

### ~~P0-4 · 里程碑級距重新設計~~ ✅ 已完成（v3.6.101）

目前 `MILESTONES = [100, 500, 1000, 2000, 5000, 10000, 50000, 100000]`，
`10000 → 50000` 是一次跳 5 倍。以現在的成長速度，訪客會在「11% → 12%」這種
幾乎不動的區間卡上好幾個月，進度條又會變回「看起來沒在動」。

**建議**：
- 在 10000 之後補 `20000 / 30000`，讓每一段大約是可預期的 1–3 個月
- 兩份 `MILESTONES` 現在不一致（`BulletinVisitorCounter` 有 2000／100000，
  舊版 `VisitorCounter` 沒有）→ 抽成單一 SSOT
- 進度條可加一行「以目前速度預計 X 月達成」，把靜態數字變成有盼頭的敘事

**驗收**：任一時間點看進度條，最近的里程碑都在「看得到的距離」內。
**成本**：S

---

## 🎨 P1 中期排程（3–7 個 session）

### ~~P1-1 · 回訪重問升級成「一鍵沿用 ＋ 只改一項」~~ ✅ 已完成（v3.6.101）

目前重問是**從頭再選一次**（身分 → 學段 → 職務 → 痛點）。對已經選過的人來說步驟偏多，
跳過率可能因此偏高。

**建議**：重問時預先帶入上次答案，第一畫面就是——

> 上次你選的是「國小班級導師」。
> 　[✅ 還是這樣，直接看新推薦] 　[✏️ 我換職務了] 　[🎯 這次想解決別的事]

第三個按鈕直接跳到痛點頁 —— 因為**痛點才是每次會變的東西**（這週在備課，下週在辦活動）。
身分一年才變一次，痛點每週都不一樣。

**驗收**：重問完成率明顯高於現行「從頭再選」版本。
**成本**：M（需要讓 `audienceWizardReducer` 支援 seed 初始狀態）

---

### ~~P1-2 · 「這批是新的」視覺標記~~ ✅ 已完成（v3.6.101）

重問的價值主張是「拿到一批你還沒看過的工具」，但訪客沒辦法一眼看出哪些是新的。
推薦引擎已經有 `excludeIds`（換一批）與 `freshness`（45 天內加分）的資料。

**建議**：在推薦卡片加標記——`🆕 上架 N 天內` / `👀 你上次沒看到的` / `🔥 這週竄升`。
讓「再點一次」的回報**看得見**，而不是要訪客自己記得上次看過什麼。

**驗收**：重問後的結果頁，至少一半卡片帶得出「為什麼這次給你看這個」的標記。
**成本**：S–M

---

### ~~P1-3 · 推薦理由的可信度分級~~ ✅ 已完成（v3.6.102）

目前 `reasons` 是人工寫死的字串。工具愈來愈多（122 個 × 6 種理由鍵），
會出現「理由寫得很漂亮但其實不太適合」的漂移，而且沒有任何機制會抓到。

**建議**：
- 為 `reasons` 加「最後複查日期」，超過 6 個月的在 Admin 標黃
- 對照 `painPoints` 與 `tags` 自動檢查理由是否至少提到一個對應概念
- CTR 長期偏低（低於同 slot 平均一半）的理由自動列入待改清單

**驗收**：Admin 能列出「這 N 條推薦理由該重寫了」。
**成本**：M

---

### ~~P1-4 · 告警分級與收斂~~ ✅ 已完成（v3.6.102，待部署 Functions）

本次只擋掉了「本機開發」。正式站的告警仍是**一顆錯誤一則 Google Chat**，
同一個 bug 在 100 個訪客身上發生就是 100 則通知 → 通知疲勞 → 真事件被淹沒。

**建議**（與既有 `ADM-P1-2 錯誤日誌工作台` 相輔）：
- 用 fingerprint（message + stack 前 3 行）合併，同一指紋 1 小時內只推一次
- 分級：`critical`（白畫面／付費路徑）立刻推、`warn` 併入每日摘要、`info` 只寫入不推
- 已知的第三方雜訊（瀏覽器擴充套件、廣告阻擋器造成的 fetch 失敗）加白名單
- 告警訊息帶「近 1 小時發生 N 次 / 影響 M 個 session / 版本 vX.Y.Z」

**驗收**：一週的告警數量下降但**沒有漏掉任何真事件**。
**成本**：M

---

### P1-5 · 本機開發環境「一定跑得起來」

`npm run dev` 目前在這台機器上起不來（`better-sqlite3` 缺原生 binding ＋ 未設 `DATABASE_URL`），
每次要驗證前端都得繞路。這會慢慢累積成「懶得驗證就直接 push」的壞習慣。

**建議**：
- 加一個 `npm run dev:web`（純 vite，不碰 Express／DB）當作前端驗證的官方入口
- `db/index.ts` 在三種連線都失敗時改為「記憶體 stub ＋ 明顯警告」，而不是 throw 讓整個 server 死掉
- README／`DEVELOPMENT_GUIDE.md` 補一段「只改前端時該跑哪一個」

**驗收**：新環境 clone 下來，`npm i && npm run dev:web` 一定能看到首頁。
**成本**：S

---

### ~~P1-6 · 工具上架流水線的自動守門~~ ✅ 已完成（v3.6.101）

本次 #122 上架時，`departments` 陷阱是靠**手動實測 6 個 profile** 才抓到的。
下次未必會想到要測。

**建議**：把 `scripts/add-tool-N.mjs` 的慣例升級成 `npm run new-tool` 的完整流程，
並在 `prebuild` 加一組守門：

- 每個工具都要有 `previewUrl` 與 `ogPreviewUrl` 且檔案真的存在
- 每個工具都要能被至少 1 個 profile 推薦到（同 P0-1 第 3 點）
- 每個工具都要有對應的 `POST_N` 手寫長文，或明確列入豁免清單
- 全 repo 不得出現「新明」（本次是人工 grep）

**驗收**：漏掉任何一項，`npm run build` 會紅燈，不會默默上線。
**成本**：M

---

## 🌐 P2 中長期願景（1–3 個月）

### P2-1 · 「這週給老師的三個工具」定期推播
把回訪重問從「被動等訪客回來」升級成「主動送到面前」。用既有的 Google Chat／PWA 通知管道，
每週依 profile 推 3 個沒看過的工具。**成本**：M–L

### P2-2 · 推薦效果的 A/B 骨架
現在調整推薦權重是「改了、上線、憑感覺」。做一個最小 A/B 框架（依 anonymous UID hash 分流），
讓「7 天 vs 14 天重問間隔」「痛點優先 vs 熱門優先」這類問題能用數據回答。**成本**：L
（與 `ADM-P1-7 推薦權重管理與安全發布` 同一條線）

### P2-3 · 工具健康度總表
122 個工具裡，有些外部連結可能已經失效、有些已經半年沒人點。
做一張「最後可用檢查／最後被點擊／CTR／是否有長文」的總表，讓退場與翻新有依據。**成本**：M

### P2-4 · 學段與職務的推薦覆蓋熱力圖
P0-2 完成後，`validate:audience` 已經會輸出 22 個 profile 的覆蓋數與 20 個痛點的工具數。
把它畫成熱力圖，一眼看出空洞在哪。**目前最薄的四個痛點**：
`professional-learning=3`、`presentation=4`、`reading-literacy=4`、`accessibility=4`
—— 這就是下一批工具最有依據的開發方向。**成本**：M

---

## 🛠️ 技術債清單（順手就改）

| 項目 | 說明 | 成本 |
|---|---|---|
| ~~`MILESTONES` 兩份不一致~~ | ✅ v3.6.101 已抽成 `lib/visitorMilestones.ts` SSOT | — |
| `VisitorCounter` 是否還需要 | 兩個訪客計數器並存，確認舊版是否仍有頁面在用，否則刪除 | XS |
| `.claude/launch.json` 的 `akai-vite` | 目前沒進版控，其他機器沒有 → 隨 P1-5 一起正式化 | XS |
| `stats.html` 每次 build 都變動 | 進 commit 造成雜訊 diff，考慮 gitignore 或穩定化輸出 | XS |
| `add-tool-N.mjs` 累積 20+ 個一次性腳本 | 抽成單一 `scripts/new-tool.mjs` + 資料檔 | S |
| Firebase 動靜態 import 混用警告 | build 每次都印一長串 `dynamically imported but also statically imported` | S |

---

## ✅ v3.6.101 完成狀態（2026-07-28 第二批）

使用者選了 **1C（三種解法全上）、2A（立刻修 #80）、3ABC（三包全做）**，已全數完成：

| 包 | 項目 | 狀態 |
|---|---|---|
| A 推薦可信度 | P0-1（改語意＋死理由偵測＋可觸及性檢查）、P1-6（上架守門） | ✅ |
| B 體感 | P0-4（里程碑 SSOT ＋ 級距 ＋ 預估時間）、P1-2（新鮮度徽章） | ✅ |
| C 閉環 | P0-3（mode 埋點）、P1-1（一鍵沿用） | ✅ |

**過程中發現的額外問題（都已修）**：
- 受 `departments` 陷阱影響的不是 2 個工具，是 **9 個**（#5 #18 #42 #46 #47 #48 #53 #72 #80）。
- **74 個工具的 OG 圖檔已存在但 `tools.json` 沒填 `ogPreviewUrl`** → 分享到 LINE／FB 用的是 1024×1024 卡片圖而非 1200×630 社群圖。是 P1-6 守門第一次執行就抓到的。
- 可觸及性檢查初版會誤判 #4／#13／#14／#21（有 `upgradeToId` 的舊版工具被家族去重刻意擋掉），已改成測「純資格」而非「競爭後排名」。

**v3.6.102 再完成**：P1-3（推薦理由體檢，含 Admin 面板）、P1-4（告警分級收斂，
Functions 待部署）。

**剩下未做**：P1-5（本機開發環境）、P2-1 ~ P2-4、技術債清單。

---

## 🎯 建議執行順序

1. **推薦可信度包**：`P0-1 + P1-6` —— 先把「新工具上架不會默默壞掉」這條防線補起來。
   這是唯一一組「不做就會再犯」的項目（P0-2 已完成，但它本身也踩到 P0-1 的陷阱）。
2. **體感包**：`P0-4 + P1-2` —— 進度條與推薦標記，訪客立刻感受得到，成本也低。
3. **閉環包**：`P0-3 + P1-1` —— 先量測再優化回訪重問，避免憑感覺調參數。
4. **工程健康包**：`P1-4 + P1-5` —— 告警降噪與開發環境，讓後續每一次改動都更省力。
5. **長線包**：`P2-1 ~ P2-4` —— 等前面四包穩了再談。

---

## 🧭 設計原則沿用

- **視覺一律走「教室公佈欄」語彙**：黃漸層便利貼 ＋ ink 厚邊 ＋ 3D offset 陰影 ＋ 微旋轉，
  不要飄向 Material／shadcn 中性風；手機 tap target ≥ 48px。
- **數字要能被讀懂**：任何百分比旁邊都要有「還差幾人 / 幾天 / 幾個」這種具體量詞。
- **新機制上線就要有量測**：沒有回路的功能等於沒有依據可以調整。
- **CI 擋得住的事就不要靠人記得**：本次的 `departments` 陷阱就是「靠人記得」失敗的案例。
