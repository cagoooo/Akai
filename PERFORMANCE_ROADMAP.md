# 網站效能優化開發進度

啟動：2026-09-08。以完成驗收作為階段切換條件；下列為工作順序及工作量預估，並非排程自動執行承諾。

| 階段 | 預估工作量 | 工作內容 | 驗收條件 | 狀態 |
|---|---|---|---|---|
| 1：減少首次下載 | 1 個開發批次 | 統一工具預載版本、移除不適用的圖片預載、舊版首頁獨立載入 | 冷啟動只下載一次工具目錄；舊版首頁切成獨立模組；卡片與舊版路由可用 | 已部署並以正式站 Chrome 驗證 |
| 2：工具介紹優先顯示 | 1–2 個批次 | 分析詳細頁依賴；標題、摘要、圖片優先，次要功能延後；資料失敗可重試 | 重複冷啟動比較文字與圖片完成時間；故障可恢復；收藏與統計正常 | 已部署並驗證重試；後續仍可深化拆分 |
| 3：部落格及字型圖片 | 1–2 個批次 | 列表與文章資料拆分評估、字型字重與圖片優先順序、音檔清單、重複頁尾 | 列表下載量與長任務下降；分類搜尋、文章與音訊正常 | 列表與字型預載改善完成，音檔及字型細分留後續 |
| 4：統計可信度 | 1–2 個批次 | 移除模擬熱門數；修正日期日均、RUM 樣本標示；查工具摘要空白及 CTR 口徑 | 指定範圍與加總一致；零筆與錯誤區分；不再冒充真實數字 | 已實作並通過本機檢查，待正式站驗收 |
| 5：完整回歸及正式站驗收 | 1 個批次 | 桌機、手機、管理端及內部連結檢查；部署後複測 | CI 通過；正式版本確認；保留結果與可回復 commit | 待開始 |

## 量測規則

- 使用相同 Chrome、尺寸、網路條件；每筆新 context，停用快取與 Service Worker。
- 本機比較使用 production build，不把 Vite 開發伺服器速度與正式站比較。
- 每組至少三筆；交錯修改前後，報告中位數與範圍。下載量、請求數與實際內容出現時間分開列。
- 骨架不算內容完成；頁面同一 SPA 內已載入的模組不算冷啟動。
- 第一階段以消除重複下載與保留功能為硬性條件，不預先承諾秒數。後續目標：代表工具頁在相同受控條件下文字/主圖完成中位數比基線下降至少 30%；未達成即繼續分析。
- 每批記錄變更、測試與限制；正式站速度要在部署後另測，不能以 localhost 數字宣稱上線改善。

## 基線

2026-09-08 正式站稽核：第一次點 #81 約 3.57 秒出現介紹；一次冷啟動主要圖片約 7.56 秒，但其他冷啟動也有約 1.4 秒，具有波動。工具目錄無版本與帶版本各下載約 459KB。全站 RUM LCP 38 筆中 7 筆超過四秒，不能將全站樣本都歸於工具頁。

完整原始稽核與截圖保留於 `.local/browser-audit/`，屬本機證據，不納入公開網站。

## 第一批實作結果（2026-09-08）

- 工具目錄預載注入 package.json 版本，與頁面 fetch 完全一致。三組冷啟動皆由兩次請求降為一次。
- 移除全路由固定預載的六張無關工具圖片；實測 preload 數量 6 → 0。
- Classic 首頁改為路由延遲載入；主 index JS 826.73 → 761.41 KB，gzip 258.79 → 238.08 KB。
- 本機 production build，Chrome 1440×1000，各三筆交錯冷啟動：#81 介紹可見時間修改前 1348/1106/1166ms，修改後 1177/1076/1147ms。中位數 1166 → 1147ms（約 1.6%），改善幅度小，不能宣稱已解決正式站慢載入。
- 六次工具頁沒有 pageerror；首頁、/classic、/tool/126/、/blog 的內容呈現通過冒煙檢查。
- TypeScript 與前後 production build 均 exit 0。build 仍有既有大 chunk 等警告，下一階段持續處理。
- 原始資料 `.local/browser-audit/phase1.json`；正式站尚未用本批版本重測。

## 第二批實作結果（2026-09-08）

- 第一批 commit 186afc3 的 Actions 34203269080 成功。正式站全新 Chrome 已確認單次工具目錄下載、零筆全域圖片預載。
- 工具長文 Markdown 獨立載入，先顯示真正摘要及使用入口；保留既有 Markdown 樣式與連結解析。
- 評論沿用既有 DeferUntilVisible，接近可見區才載入程式及評論查詢；主圖設為高優先權。
- 工具下載失敗顯示獨立錯誤與重試按鈕；不存在的 ID 仍顯示 NotFound。
- 同機 production build，各三筆交錯 Chrome 冷啟動，1440×1000，快取停用。摘要中位數 1314 → 967ms（26.4%）；完整排版介紹 1336 → 1143ms（14.4%）。檢查主要圖片時已等待介紹完成，所以 imageReady 是「介紹與圖片皆就緒」的上界，不是圖片獨立完成時間。
- 上述為少量 localhost 樣本，不等於正式站改善幅度。距離完整內容至少 30% 的目標仍有差距。
- 故意暫停 Markdown chunk 時摘要/使用入口正常；捲到評論可顯示表單；資料 503 後按重試恢復；不存在 ID 正確區分。
- 型別檢查、production build、lint 通過；DeferUntilVisible / ReviewForm 共 11 個既有單元測試通過；新增 2 個 production E2E 測試通過並納入 CI。
- 原始量測 `.local/browser-audit/phase2.json`。本機測試曾遇到 4173 被其他專案占用，已使用獨立 4189 及正確工作目錄重跑通過；不視為網站缺陷。

## 第三批實作結果（2026-09-08）

- 第二批 afc1daa 已部署成功，正式站工具資料 503 故障注入後可按重試恢復完整內容；Actions 34204270744 的八個 E2E 通過。
- 部落格列表使用現有 POSTS_INDEX。全文只在開始搜尋時載入，保留 body 搜尋；載入中及失敗時明示目前是部分搜尋，失敗可重試。
- mini posts 與平台篩選共用工具 query，不再額外 fetch 一次工具目錄；沒有搜尋時也不建立 Fuse 全文索引。
- Chrome 三組冷啟動均確認：首訪 posts 約 682KB（gzip 約 270KB）下載取消，tools.json 請求 2 → 1；輸入搜尋後才下載全文。
- 文章列表卡片數前後皆 124（不含上方三篇主打文章），PIRLS 搜尋前後皆 51 筆；平台篩選與文章開啟通過新增 E2E。
- 部落格頁尾 2 → 1。移除兩個舊 Inter preload，保留 @font-face 讓需要該字型的頁面仍可載入，沒有更換字型或刪減字重。
- 第一輪包含 load 等待的本機秒數中位數 3066 → 3254ms，波動較大，不能宣稱整頁時間已改善。首筆與部分回歸測試同時執行，秒數僅作診斷參考。主要驗收是確定移除不必要請求及保留搜尋。
- TypeScript、lint、production build、prebuild 資料管線與新增 E2E 通過；原始量測在 `.local/browser-audit/phase3.json`，另外以 DOM 可見為完成點的量測在 `phase3-dom.json`。

- 第三批獨立 DOM 量測：列表卡片可見時間前 3021/3609/3652ms、後 3181/3630/3208ms，中位數 3609 → 3208ms（約 11.1%）。只有三筆本機樣本，不等於正式站改善幅度。

## 第四批實作結果（2026-09-08）

- 第三批 d0771c1 已部署成功，Actions 34205150390 九個 E2E 通過；正式站確認列表零全文 chunk、一次工具目錄、單一頁尾，搜尋後才下載全文且 PIRLS 仍有 51 筆。
- 移除部落格模擬閱讀數及「熱搜／老師最愛」徽章；改為按發布日期排序的近期文章。
- 工具摘要空白根因：callable 寫入數字文件 ID、不寫 toolId 欄位；管理端卻只讀欄位。新增解析器相容數字 ID、舊 tool_N 與舊 toolId 欄位，合併過渡期文件。
- 工具期間排行只取每日明細，不把缺少每日明細的全期累計混入；分開顯示載入中、讀取失敗、期間無可統計資料。
- 日期範圍以日曆日期計算，今天為一天、8/10–9/8 為三十天；日均及上一期間比較隨之校正。日曆合計只加總圖中最近三十天。
- RUM 明示為多種指標的樣本數，不再乘四換算瀏覽人數；推薦指標改稱「點擊／曝光比」，說明重複點擊可使比例超過 100%。沒有改寫任何既有統計文件。
- 新增三個單元測試涵蓋日期、新舊文件合併及無效數字；部落格 E2E 加入不顯示假閱讀數的檢查。TypeScript、lint、production build 及 prebuild 通過。
