# 第一輪網站優化驗收

日期：2026-09-08。正式站：https://cagoooo.github.io/Akai/ 。驗收程式版本：`8bc62ea`。

## 驗收結論

已完成本輪首頁、工具介紹、部落格、手機／平板版面與後台的代表流程回歸。四批修正均已部署，未發現這些修正造成的新 JavaScript 執行錯誤。工具資料重複下載、列表預先下載全文、假閱讀數、後台統計解析及圖表溢出等問題已解決。

這是本輪修改的驗收，不代表所有外部工具應用都完成測試，也不代表所有效能目標已達成。尤其部落格列表與共用字型仍有明顯改善空間。

## 正式站冷啟動

以可見的真正 Google Chrome 執行。每筆建立全新 context、封鎖 Service Worker、停用 HTTP 快取。15 筆初始 localStorage、sessionStorage、可讀 cookies 均為零。不清除使用者原有 Chrome 個人資料，也不能清除 CDN 或作業系統 DNS 快取。

尺寸 1440×1000；同機依序執行三輪，不同頁面各三筆。以指定實際內容可見為完成點，不把骨架當內容完成；不是 LCP 或整頁全部資源完成時間。

| 頁面 | 完成點 | 三筆時間（ms） | 中位數 |
|---|---|---|---|
| 首頁 | 第一張工具卡 | 2642 / 2275 / 2461 | 2.46 秒 |
| 工具 #81 | 詳細介紹的 Markdown 標題可見 | 1905 / 1079 / 1296 | 1.30 秒 |
| 工具 #126 | 詳細介紹的 Markdown 標題可見 | 1254 / 1162 / 1291 | 1.25 秒 |
| 部落格列表 | 列表文章卡片可見 | 3374 / 2793 / 3343 | 3.34 秒 |
| #81 教學文章 | 文章容器可見 | 2128 / 1674 / 1601 | 1.67 秒 |

結果：15 筆均無 pageerror、無整頁水平溢出，工具目錄均只請求一次。首頁、工具頁與部落格列表均未下載手寫文章全文 chunk；文章頁下載一次。

原始稽核的 7.56 秒是一次主要圖片完成樣本，與本表的內容完成點、樣本分布不同，因此不能直接拿它計算整體改善百分比。各批本機同條件比較另見 PERFORMANCE_ROADMAP.md。

## 功能與版面

- 390px 與 768px，首頁、工具 #81、部落格列表、文章及匿名管理登入頁共十組，均無整頁水平溢出，且各只有一個頁尾。手機部落格畫面已目視檢查。
- 首頁 126 個工具入口；七類卡片數為 41 / 26 / 22 / 14 / 13 / 7 / 3，均符合畫面標示。
- 工具 PIRLS 搜尋四筆；家族樹展開／收合可操作。
- 五種部落格平台切換與 URL 同步正常；PIRLS 全文搜尋 51 筆，文章連結可開啟且單一頁尾。
- 從本次頁面重新取得的 126 個工具入口與 127 個文章入口，共 253 個網址全部 HTTP 200 且回傳 HTML。這是網址存活驗證，不是 253 頁每項互動都測過。
- 最近一次程式部署 Actions `34206831467` 成功，包含九個 production 瀏覽器回歸測試；資料重試、延遲 Markdown、按需全文搜尋及無假閱讀數都由既有回歸測試涵蓋。

## 管理後台

使用已登入的 Chrome 工作階段，與前端匿名冷啟動分開判讀；沒有提交、還原、刪除或改寫管理資料。

- 30 天天數正確；切換為七天顯示七天，期間流量 264、日均 37.7。
- 還原三十天後，期間流量與圖中日期總計同為 892 次（即時數值，之後會隨訪問改變）。
- 工具圖表實際 CSS 高度 400px，父容器亦 400px，分頁不再被遮住。
- 推薦指標顯示「點擊／曝光比」並解釋重複事件；RUM 使用「指標樣本總數」。
- 驗收結束已還原三十天與總覽分頁。

## 下一輪優先順序

| 優先 | 待辦 | 目前證據與驗收方向 |
|---|---|---|
| 高 | 部落格列表減少主執行緒工作 | 每筆觀察到約 794–807ms 最長任務。分析卡片渲染、樣式及版面計算，評估分批顯示；保留分類、搜尋與可分享 URL。 |
| 高 | 字型依頁面拆分／減少字重 | 共用 Google Fonts 樣式表解壓後約 1.11MB，傳輸約 297KiB。先量測實際字型使用，再拆文章專用字型，避免憑猜測更換外觀。 |
| 中 | 文章全文與工具資料進一步拆分 | 開單篇文章仍會下載約 683KB 的手寫全文集合；工具目錄約 459KB，每張工具頁仍取全目錄。需從產生管線拆 metadata／單篇資料並保持索引同步。 |
| 中 | 減少 Firebase 與共享入口的首屏依賴 | 各頁觀察到約 499KB Firebase chunk、491KB 主入口 JS（解壓大小）。需沿實際依賴圖拆分，保留匿名身分、收藏與統計行為。 |
| 低 | Podcast 存在清單 | #81 文章三次都對不存在音檔 HEAD 得到 404，播放器會隱藏；不是文章失效。改成建置時產生音檔清單可消除此請求。 |
| 低 | 其他原稽核項目 | 管理入口巢狀連結、部分過時文案及地區名稱正規化尚未納入本輪修改。 |

原先「完整工具內容中位數下降至少 30%」的目標尚無足夠同條件正式站證據可宣稱達標。本輪確認減少不必要下載及修復功能，下一輪繼續以相同完成點做比較。

## 本機證據

`.local/browser-audit/final-cold.json`：15 筆冷啟動、長任務、資源與錯誤。

`.local/browser-audit/final-functional.json`：響應式及互動結果。

`.local/browser-audit/final-route-list.json`、`final-routes.json`：本次 253 個內部入口與 HTTP 結果。

`.local/browser-audit/final-390-blog.png` 等：本次桌機、手機與平板截圖。

原始證據位於忽略目錄，只保留於本機；本文件不包含老師許願內容或登入資料。
